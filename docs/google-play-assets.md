# Dovra — Play Console assets still needed (developer-side)

Nothing below can be generated honestly inside the repo; all require real
screenshots from a device/emulator.

## Already in repo (reusable)

- `website/assets/dovra-icon-512.png` — 512×512 high-res icon, use as-is for
  the Play **app icon** (512×512, 32-bit PNG required).
- `website/assets/dovra-feature-graphic.png` — 1024×500 draft feature
  graphic (icon + brand band, no text). Acceptable as-is or add the tagline
  in Play's editor; keep any text out of the central safe zone.
- Launcher adaptive icon (`mipmap-anydpi-v26` + PNG densities) — ships in
  the app; no text, no clipping.

## To capture (minimum: 2 phone screenshots; recommended: 4–8)

Required sizes: 16:9 or 9:16, each 320–3840 px. Capture at 1080×2400.

| # | Screen | Caption suggestion |
|---|---|---|
| 1 | Home (hero + recent documents) | “Your documents, on your device” |
| 2 | Document reader | “Clean reading, fully offline” |
| 3 | Conversion flow (target picker) | “Convert to the format you need” |
| 4 | Conversion progress/success + Save | “Honest quality labels, easy export” |
| 5 | Supported-format note / simplified label close-up | “Full or Simplified — always labelled” |
| 6 | Settings (theme + offline strip) | “Private by design: no account, no ads” |
| 7 | Dark mode (any screen) | “Light & dark themes” |

Optional tablet (7″ + 10″) sets reuse the same 7 concepts.

## Text assets (done, in `docs/google-play/`)

App name, short + full descriptions (EN + ID) are finalized in-repo. Paste
into Store listing → Main store listing.
